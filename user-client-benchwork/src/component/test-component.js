import React from "react";

const TestComponent = ({ onClick }) => (
    <div>
        <input onClick={onClick} type="button" value="DOWNLOAD THE PDF :)"></input>
    </div >
);

export default TestComponent; 
